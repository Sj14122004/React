CreateRoot Method create the document object model(DOM) like the browser.

Broser already create the dom but it also create the dom because it  compare its dom with the browser dom and update only those things that are updated in the user interface.but browser create new dom again if some changes done and repaint it that is called page reload.but in the dom created by createRoot we can track the changes in the dom and change there only that is called virtual dom

learn react fibre form this github link ==>https://github.com/acdlite/react-fiber-architecture.

React fibre is used to update the react virtual DOM