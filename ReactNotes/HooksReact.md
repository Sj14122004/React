Hooks are a special function in react that let us use react features like state, lifecycle methods,context and refsinside the functional components without writing class components.

hooks are the built in react function that allow other function to manage the react state and other features

(babel learn what is babel ,hooks)

1:=> useState ==> declare the state variable that we can update directly.baiscally it is responsible to update the state

useState always return the array of two element.At first index it return the variable and at second index it return the function.inside that function we have write we want to do